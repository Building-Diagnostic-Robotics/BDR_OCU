<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>odrive_can</name>
  <version>0.0.1</version>
  <description>
    Vendored interface-only snapshot of the upstream odrive_can msgs/srvs.
    The full driver lives in the robot workspace (pilot_ws); BDR_OCU only
    needs the message contract to subscribe/publish over DDS. Keeping this
    snapshot in-tree decouples the OCU build (and CI) from pilot_ws.
  </description>
  <maintainer email="eng@bdr.com">BDR Engineering</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <build_depend>rosidl_default_generators</build_depend>

  <exec_depend>rosidl_default_runtime</exec_depend>

  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
